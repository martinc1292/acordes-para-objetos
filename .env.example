# Browser-safe Supabase project credentials.
# Get these from the Supabase project API settings.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server/admin-only values for local scripts and maintenance jobs.
# Never expose the service role key in browser code.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
